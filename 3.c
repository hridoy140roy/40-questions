#include <stdio.h>
int main() {
    float radius = 5.2;  // circle r radius 5.2 dia ase
    float area;
    float PI = 3.14;// pi value 

    // circle r area ulua furmula di calculate korisu
    area = PI * radius * radius;

    printf("Area of the circle = %f", area);

    return 0;
}